// src/services/data/competencyService.ts
import { supabase } from '~/server/db/supabase';

// Functions for working with the EU skills dataset
export async function searchCompetencies(query: string) {
  // Placeholder for future implementation
  return [];
}

